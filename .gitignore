# Dependencies
node_modules/
.pnpm-store/

# Build output
out/
dist/
release/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
# Legacy persistent runtime dir name (CODEX_RUNTIME_DIR_NAME in main); keep.
.orca-runtime/

# Runtime artifacts (agent handoff briefings; written to userData at runtime,
# but tests that mock app.getPath emit them into the repo root).
# Legacy directory name kept for compatibility with main process paths.
orca-handoffs/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
